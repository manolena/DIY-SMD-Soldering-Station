If you want to make your own .bmp files, you should prepare them with any appropriate programm or simply with Windows Paint. 
The size of image shoule be 320x240 pixels, 24bit color and shaved as bmp file. Before saving, you must invert colors of image,
otherwise it will be shown as negative image.
